#include <stdio.h>
#include <stdlib.h>

void main(void)
{
    printf("Hello world!\n");
    const int length=10;
    const int width=5;
    int area;
    area=length*width;
    printf("area is equal %d",area);
    return 0;
}
